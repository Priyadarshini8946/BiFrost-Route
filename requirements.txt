# Bifrost Route · Layer 1 (Data Layer) Python dependencies
boto3>=1.34,<2            # DynamoDB (Local + AWS)
redis>=5.0                # Redis client
numpy>=2.1,<3            # embedding vectors / kNN math (cp313 wheels; 1.26.4 built from source and broke)
scikit-learn>=1.4,<2      # TF-IDF semantic embedder (offline, deterministic)
duckdb>=1.0,<2            # local analytics warehouse (Redshift stand-in)
pymysql>=1.1,<2           # MySQL client
python-dotenv>=1.0        # .env loading
tabulate>=0.9             # result tables
pytest>=8.0               # smoke tests (CI-ready)