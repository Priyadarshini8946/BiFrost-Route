# Bifrost Route — pytest configuration
# Default suite = completed layers (1 and 2). Layer 3+ tests are excluded
# until those layers are built and green; they run once Layer N lands.
[pytest]
testpaths = tests/test_layer1.py tests/test_layer2.py tests/test_layer4.py
addopts = -q